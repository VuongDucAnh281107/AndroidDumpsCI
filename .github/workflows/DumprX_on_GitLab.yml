name: DumprX on GitLab
 
on:
  workflow_dispatch:
    inputs:
      ROM_URL:
        description: "Direct Link of the ROM/Firmware to Dump"
        required: true
      EXTRA_CMD:
        description: "Any Extra Command to Run Before Dump"
        required: false

defaults:
  run:
    shell: bash

env:
  # ROM
  ROM_URL: ${{ github.event.inputs.ROM_URL }}

  # Gitlab
  PUSH_TO_GITLAB: true

  # Extra Command
  EXTRA_CMD: ${{ github.event.inputs.EXTRA_CMD }}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
       - name: Cleanup
         uses: rokibhasansagar/slimhub_actions@main

       - name: Checkouts
         uses: actions/checkout@v4

       - name: Clone Our Firmware Dumper
         run: |
              git clone https://github.com/Van-Firmware-Dumps/DumprX.git DumprX

       - name: Setup DumprX's Environment
         working-directory: DumprX
         run: |

              sudo bash setup.sh
              sudo apt install -y f2fs-tools neofetch
              mkdir -p ~/.ssh
              ssh-keyscan -t rsa github.com >> ~/.ssh/known_hosts
              ssh-keyscan -t rsa gitlab.com >> ~/.ssh/known_hosts
              git config --global user.name "VanVuong41429"
              git config --global user.email "vanvuong41429@gmail.com"

       - name: Setup Config envs
         working-directory: DumprX
         run: |
              echo "${{ secrets.GITLAB_TOKEN }}" > .gitlab_token  # Only for Gitlab
              echo "Van-Firmware-Dumps" > .gitlab_group  # Only for Gitlab
              echo "gitlab.com" > .gitlab_instance  # Only for Gitlab
              echo "${{ secrets.PRIV_SSH_KEY }}" > ~/.ssh/id_rsa  # Only for Gitlab
              echo "${{ secrets.BOT_TOKEN }}" > .tg_token
              echo "@VanFirmwareDumps" > .tg_chat
              chmod 600 ~/.ssh/id_rsa

       - name: Run the Extra Command
         working-directory: DumprX
         run: |
              $EXTRA_CMD
              wget -O firmware.zip 'https://huang2728.sharepoint.com/sites/hk27/_layouts/15/download.aspx?UniqueId=a2c73e00-ce03-4201-818a-866c7894ba1a&Translate=false&tempauth=v1..CgoKBHNuaWQSAjY0EgsIyqPLkrG4kT0QBRoOMjAuMTkwLjE0NC4xNzEqLC8zQUE0MXZWcnRCOURaYXlWZGJLU3MxcjdDZ0czZFlrN1ZxTlJVaGpXc2c9MIMBOAFCEKE7vJ9FEAAwjv_Eh9po-LdKEGhhc2hlZHByb29mdG9rZW5SCFsia21zaSJdcikwaC5mfG1lbWJlcnNoaXB8MTAwMzIwMDI0YjNjZDFjYkBsaXZlLmNvbXoBMoIBEglSZlvavEgqTRGaIgt7wmgskKIBHWhrMUBodWFuZzI3Mjgub25taWNyb3NvZnQuY29tqgEQMTAwMzIwMDI0QjNDRDFDQrIBDmFsbGZpbGVzLndyaXRlyAEB.m8iLpl8tnQ7sx1nkKiWJKF4TNdSYg955EIIMyfpzaRo&ApiVersion=2.0'
              7z x -y -o'firmware' firmware.zip && rm firmware.zip
              zip -r firmware.zip firmware/system-image && rm -rf firmware

       - name: Start Dumping
         working-directory: DumprX
         run: |
              ./dumper.sh "$ROM_URL"
